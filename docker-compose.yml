services:
  gitpet:
    build: .

    container_name: gitpet

    ports:
      - "3000:3000"

    environment:
      GEMINI_API_KEY: ${GEMINI_API_KEY}
      APP_URL: http://localhost:3000
      NODE_ENV: production

    restart: unless-stopped